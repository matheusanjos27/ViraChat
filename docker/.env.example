# Copie para docker/.env e ajuste (só Evolution / dev)

EVOLUTION_API_KEY=troque-por-uma-chave-longa
EVOLUTION_DB_PASSWORD=troque-senha-postgres
EVOLUTION_PORT=8080
# URL pública ou IP do VPS (sem barra no final)
EVOLUTION_SERVER_URL=http://SEU_IP_OU_DOMINIO:8080

# Produção completa (app + Evolution + Caddy): use .env.prod.example
# cp .env.prod.example .env
# ver DEPLOY-VPS.md
